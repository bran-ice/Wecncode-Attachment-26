# Reflection note on writing JSON files

## Summary
In this exercise i learnt the basics of writing a JSON files.
This involved importing json files with import json.
Reading the json file with json.load(), this converts a string into a usable python dictionary.
converting a python dictionary into a json file using json.dumps
I applied indentation to increase neatness and readability of the credentials.

To add a new skill onto the json file already created, first convert the file to a dict for reading "r"  and json.dump.
To save the added skill into the existing json file use json.dump