learn how to read information from files and write results back out. You'll see how to open files safely with with open so they always close, how to pull rows out of a comma-separated (CSV) file and turn those fields into numbers you can do math with, and how to catch problems like a missing file or a bad number so the program doesn't crash using try/except. We'll put those pieces together into an Expense Splitter that reads a receipt, applies a tip and head count, and saves a per-person summary to its own file.